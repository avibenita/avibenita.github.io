---
description: After completing a coding request, commit changes to git unless the user opts out.
alwaysApply: true
---

# Commit after completing requests

When you finish a user’s coding task in this repository (edits that are meant to be kept):

1. **Commit** the relevant changes to `git` on the current branch with a clear, conventional message (what changed and why).
2. Do **not** leave the working tree dirty with those changes unless the user explicitly asked not to commit, to pause, or to only show a diff.
3. If there is nothing to commit, say so briefly.
4. **Do not push** unless the user asks to push.

This applies after substantive edits; trivial exploratory steps can be squashed into one commit when the task is done.
